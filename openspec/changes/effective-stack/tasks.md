## 1. エフェクティブスタック計算ロジック

- [ ] 1.1 `src/lib/effective-stack.ts` を作成し、`calculateEffectiveStack(myStackBB, opponentStackBB)` 関数を実装する
- [ ] 1.2 エフェクティブスタック計算のテストを作成する（自分が小さい、相手が小さい、同値、相手0のケース）

## 2. 状態管理

- [ ] 2.1 `PokerChipCalculator.tsx` にエフェクティブスタック機能のトグル状態（デフォルトOFF）と相手スタックBB値の state を追加し、sessionStorage で永続化する
- [ ] 2.2 自分のスタックBB変更・相手スタック変更に応じたエフェクティブスタック再計算ロジックを統合する

## 3. UI 実装

- [ ] 3.1 Total Stack セクション直下にエフェクティブスタックのトグルボタンを追加する
- [ ] 3.2 トグルON時に相手スタックBB入力フィールドとエフェクティブスタック表示を展開するUIを実装する
- [ ] 3.3 エフェクティブスタック値を `Effective Stack: XX BB` の形式で表示する（整数は小数なし、端数は小数1桁）

## 4. テスト

- [ ] 4.1 トグル状態と相手スタック値の sessionStorage 永続化テストを作成する
- [ ] 4.2 表示フォーマット（整数/小数）のテストを作成する
