## 1. ポットオッズ計算ロジック

- [ ] 1.1 `src/lib/pot-odds.ts` を作成し、`PotOddsInput` 型、`PotOddsResult` 型、`calculatePotOdds` 関数を実装する
- [ ] 1.2 ポットオッズ計算のテストを作成する（標準ケース、コール額0、ポット0、ポットサイズと同額ベットのケース）
- [ ] 1.3 必要エクイティがポットオッズと同値であることのテストを作成する

## 2. 状態管理

- [ ] 2.1 `PokerChipCalculator.tsx` にポットオッズ機能のトグル状態（デフォルトOFF）、ポットサイズBB、コール額BBの state を追加し、sessionStorage で永続化する
- [ ] 2.2 ポットサイズ・コール額の変更に応じたポットオッズ再計算ロジックを統合する

## 3. UI 実装

- [ ] 3.1 Total Stack セクション下にポットオッズのトグルボタンを追加する
- [ ] 3.2 トグルON時にポットサイズ入力（BB単位）とコール額入力（BB単位）を展開するUIを実装する
- [ ] 3.3 Pot Odds と Required Equity をパーセント表示する（整数は小数なし、端数は小数1桁）

## 4. テスト

- [ ] 4.1 トグル状態と入力値の sessionStorage 永続化テストを作成する
- [ ] 4.2 表示フォーマット（整数/小数パーセント切り替え）のテストを作成する
