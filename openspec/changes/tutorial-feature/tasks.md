## 1. チュートリアルコアロジック

- [x] 1.1 `TUTORIAL_VERSION` 定数とステップ定義（ステップID、対象セレクタ、タイトル、説明テキスト、ポップオーバー位置）を作成する
- [x] 1.2 `useTutorial` カスタムフックを実装する（現在ステップ、次へ/前へ/スキップ/完了、localStorage バージョン永続化、バージョン比較による自動表示判定）
- [x] 1.3 `useTutorial` のテストを作成する（初回表示、バージョン更新時の再表示、最新バージョン完了済みで非表示を含む）

## 2. チュートリアル UI コンポーネント

- [x] 2.1 `TutorialOverlay` コンポーネントを実装する（セミトランスペアレント背景、対象要素のハイライト）
- [x] 2.2 `TutorialPopover` コンポーネントを実装する（説明テキスト、次へ/前へ/スキップボタン、×閉じるボタン、ステップインジケーター）
- [x] 2.3 ウェルカムステップ（対象要素なし、中央表示）を実装する
- [x] 2.4 キーボード操作（Enter/→ で次へ、← で前へ、Escape でスキップ）を実装する
- [x] 2.5 チュートリアル UI のテストを作成する

## 3. 既存コンポーネントの修正

- [x] 3.1 チュートリアル対象要素に `data-tutorial` 属性を付与する（blind-input, chip-counter, add-chip, record-button）
- [x] 3.2 ヘッダーにヘルプボタン（`?` アイコン、`aria-label="チュートリアルを表示"`）を追加する

## 4. 統合

- [x] 4.1 `PokerChipCalculator` に `useTutorial` と `TutorialOverlay` を統合する
- [x] 4.2 初回アクセス時の自動表示とヘルプボタンからの再表示を接続する
- [x] 4.3 E2E 的な統合テストを作成する（初回表示→ステップ進行→完了→再表示されない→バージョン更新で再表示の確認）
